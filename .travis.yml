language: node_js
node_js:
  - "10.16.0"
before_install: dpkg --compare-versions `npm -v` ge 6.9.2 || npm i -g npm@^6.9.2
install:
  - npm install
script:
  - npm test
  # - npm run build:clean
  # - npm run build
